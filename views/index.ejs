<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Generator</title>

    <style>
        body {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            background: linear-gradient(to right, #D3E3F8, #E0ECF8);
            font-family: 'Arial', sans-serif;
        }

        h1 {
            text-align: center;
            color: #394240;
        }

        form {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
            max-width: 400px;
            margin: 0 auto;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #394240;
        }

        input, select {
            width: 100%;
            padding: 10px;
            margin-bottom: 16px;
            border: none;
            border-radius: 8px;
            background: rgba(255, 255, 255, 0.5);
            color: #394240;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        button {
            background: #68D391;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background 0.3s;
        }

        button:hover {
            background: #4C9F68;
        }

        img {
            max-width: 100%;
            border-radius: 8px;
            margin-top: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>
    <h1>Face Generator</h1>
    <div style = "display : flex ; flex-direction: row;">
    <form method="post" action="/">
        <!-- <input type="text" id="prompt" name="prompt" required> -->
        <!-- Add dropdown for nose -->
        <label for="nose">Nose Size:</label>
        <select id="nose" name="nose">
            <option value="big">Big</option>
            <option value="medium">Medium</option>
            <option value="small">Small</option>
            <option value="very-small">Very Small</option>
            <!-- Add more options as needed -->
        </select>

        <!-- Add dropdown for eyes -->
        <label for="eyes">Eyes Size:</label>
        <select id="eyes" name="eyes">
            <option value="big">Big</option>
            <option value="medium">Medium</option>
            <option value="small">Small</option>
            <option value="very-small">Very Small</option>
            <!-- Add more options as needed -->
        </select>

        <!-- Add dropdown for eyebrows -->
        <label for="eyebrows">Eyebrows Size:</label>
        <select id="eyebrows" name="eyebrows">
            <option value="big">Big</option>
            <option value="small">Small</option>
            <option value="medium">Medium</option>
            <option value="large">Large</option>
            <!-- Add more options as needed -->
        </select>
        

        <!-- Add dropdown for skin tone -->
        <label for="skintone">Skin Tone:</label>
        <select id="skintone" name="skintone">
            <option value="fair">Fair</option>
            <option value="medium">Medium</option>
            <option value="dark" >Dark</option>
            <!-- Add more options as needed -->
        </select>

        <!-- Add dropdown for lips -->
        <label for="lips">Lips Size:</label>
        <select id="lips" name="lips">
            <option value="big">Big</option>
            <option value="medium">Medium</option>
            <option value="small">Small</option>
            <option value="very-small">Very Small</option>
            <!-- Add more options as needed -->
        </select>

        <!-- Add dropdown for gender -->
        <label for="gender">Gender:</label>
        <select id="gender" name="gender">
            <option value="male">Male</option>
            <option value="female">Female</option>
            <!-- Add more options as needed -->
        </select>

        <!-- Add dropdown for age -->
        <label for="age">Age:</label>
        <select id="age" name="age">
            <option value="child">Child</option>
            <option value="adult">Adult</option>
            <option value="old">Old</option>
            <!-- Add more options as needed -->
        </select>

        <button type="submit">Generate Image</button>
    </form>
<div style = "filter: blur(10px)">
    <% if (image_path) { %>
        <img src="<%= image_path %>" alt="Generated Image">
    <% } %>

</div></div>
</body>
</html>
